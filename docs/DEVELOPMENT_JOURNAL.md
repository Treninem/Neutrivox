# Журнал разработки Neutrivox

Этот файл является постоянным журналом проекта. После каждого значимого этапа сюда добавляются: что сделано, что проверено, планы, проблемы и причины решений.

---

## 2026-08-27 — Большой этап: фундамент полноценного редактора проекта

### Что сделано
- Добавлена отдельная модель метаданных проекта и статусов готовности.
- Добавлена структурированная система результатов проверки с уровнями Info, Warning и Error.
- Реализован сервис проверки проекта: имя проекта, состав оборудования, известность профилей, каналы и целостность соединений.
- Добавлен сервис сохранения и загрузки проектов в собственном формате `.neutrivox` на основе JSON.
- Добавлен сервис управления соединениями устройств с защитой от дубликатов, самосоединений и ссылок на отсутствующие устройства.
- Создан фундамент доменной модели лицензирования и редакций программы.
- Добавлена подробная дорожная карта продукта.

### Что изменилось
Neutrivox теперь развивается не только как интерфейс, а как набор отдельных слоёв: данные проекта, оборудование, соединения, проверка, сохранение и лицензирование. Это позволяет последовательно подключать интерфейс к готовой логике и не смешивать всё в одном окне.

### Что проверено
Логика сервисов использует только текущую модель проекта и не требует доступа к реальному оборудованию. Это позволяет безопасно тестировать проектирование отдельно от физических контроллеров.

### Какие проблемы обнаружены
Главное окно приложения всё ещё содержит слишком много временной логики. Следующим большим этапом нужно переносить её в отдельные представления и сервисы, а не продолжать увеличивать один файл.

### Какое решение принято и почему
Выбрана модульная структура. Сначала строится полноценная логика редактора и сохранения, затем визуальный редактор схемы и реальные профили оборудования. Это снижает риск дорогостоящей переделки, когда список поддерживаемых устройств расширится.

### Что делать дальше
1. Разделить интерфейс на самостоятельные страницы/компоненты.
2. Подключить сохранение и открытие проектов к интерфейсу.
3. Создать полноценное визуальное рабочее поле схемы.
4. Добавить панель свойств устройств и соединений.
5. Добавить автоматические тесты для новых сервисов.
6. После проверки официальных источников начать реальные профили поддерживаемого оборудования.

---

## Правило ведения журнала
При каждом крупном изменении обязательно фиксируются: сделанное, изменения, проверка, проблемы, решение с причиной и дальнейший план.
